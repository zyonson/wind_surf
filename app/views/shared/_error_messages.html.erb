<% flash.each do |message_type, messages| %>
  <div class="alert alert-danger">
    The form contains <%= pluralize(messages.count, "error") %>
  </div>
  <% messages.each do |message| %>
    <div class="alert alert-<%= message_type %>"><%= message %></div>
  <% end %>
<% end %>
